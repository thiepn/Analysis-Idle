# Analysis Idle

A browser idle game about building Understanding from the foundations of Analysis I.

## Run Locally

Open `index.html` through a local static server. The project has no build step and uses plain HTML, CSS, and JavaScript modules.

## GitHub Pages

This project can be hosted directly from the repository root with GitHub Pages.
