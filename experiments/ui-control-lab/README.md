# UI control lab

- Hypothesis: tap-to-assign and plus/minus controls with presets outperform sliders and drag-only tokens.
- Parameters: actions, precision burden, touch burden, keyboard and screen-reader feasibility.
- Output: embedded in `attention-model-results.json`; prototype under `prototypes/phase-0/`.
- Conclusion: essential allocation works without dragging or fine pointer precision.

