# Phase 0 design lab

Run `npm run phase0:report` to reproduce all machine-readable data and comparison reports. Run `npm run phase0:all` for report generation, repeatability, type checks, tests, artifact validation, prototype smoke tests, and screenshot checks.

All current models are deterministic and timestamp-free. Their scores are explicit heuristics or mathematical enumerations—not claims that a design is fun.

